# <Topic> Reference

Settled values only. Anything still under discussion belongs in `decisions.md` or
`handoff.md` — this file is for lookup, and its usefulness depends on being trusted
without checking.

<!-- verified: YYYY-MM-DD -->

## Parameters

| Name | Value | Unit | Where used | Source |
|---|---|---|---|---|
| `<name>` | <value> | <unit> | `<file>` | session NNN / measurement / paper |

## Paths

| Purpose | Path | Notes |
|---|---|---|
| <what> | `<path>` | <read-only? generated?> |

The `Source` column is what makes a value auditable. A number with no provenance
cannot be distinguished later from one that was guessed.
